[gd_resource type="ShaderMaterial" load_steps=3 format=2]

[ext_resource path="res://assets/palette.png" type="Texture" id=1]

[sub_resource type="Shader" id=13]
code = "shader_type canvas_item;

uniform sampler2D color_palette;

void fragment() {
    vec4 sprite_color = texture(SCREEN_TEXTURE, SCREEN_UV);
    vec4 palette_color = texture(color_palette, vec2(sprite_color.r));
    COLOR = vec4(palette_color.rgb, sprite_color.a);
}"

[resource]
shader = SubResource( 13 )
shader_param/color_palette = ExtResource( 1 )
